<!DOCTYPE html>
<html>
<head>
  <title>Email Verification</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="card shadow">
          <div class="card-body text-center p-5">
            <% if (messages) { %>
              <h2 class="text-success mb-4">✅ Verification Successful</h2>
              <p><%= messages %></p>
              <a href="/login" class="btn btn-primary mt-3">Go to Login</a>
            <% } %>
            
            <% if (messages.error) { %>
              <h2 class="text-danger mb-4">❌ Verification Failed</h2>
              <p><%= messages.error %></p>
              <a href="/resend-verification" class="btn btn-secondary mt-3">Resend Verification Email</a>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>